{
  "name": "shoqnohu",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "db:sync": "npm run db:push -w server",
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "test": "npm run test -w client && npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
